# prod 프로파일 전용 설정 (k8s 배포: SPRING_PROFILES_ACTIVE=prod)
# 콘솔 로그를 ECS(Elastic Common Schema) JSON으로 출력 → Loki/Grafana에서 구조화 조회.
# Spring Boot 내장 structured logging(3.4+) 사용 — 추가 의존성/logback 설정 불필요.
# 로컬(local 프로파일)은 사람이 읽기 좋은 기본 콘솔 포맷을 그대로 사용한다.
logging:
  structured:
    format:
      console: ecs
